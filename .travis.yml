language: r

env:
  global:
      - secure: A_LONG_ENCRYPTED_STRING

r_github_packages:
  - rstudio/bookdown

install: true

before_script:
  - chmod +x ./_build.sh
  - chmod +x ./_deploy.sh

script:
  - ./build.sh
  - ./deploy.sh